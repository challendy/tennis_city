%h2 Atp Players
#players
  %table
    %thead
      %tr
        %th Atp Rank
        %th Name
        %th Country
        %th Points
        %th Tournaments Played
    %tbody
      -@players.each do |player|
        %tr
          %td=h player.atp_rank
          %td=link_to"#{player.name}", player_path(player)
          %td=h player.country
          %td=h player.points
          %td=h player.tournaments_played